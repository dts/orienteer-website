.course-list {
  .item {
    display: block;
    
    overflow: hidden;
    position: relative;
    width: 100%;
    height: 4em;
    padding-left: 55px;

    h2 { font-size: 1em; }

    img {
      position: absolute;
      display: block;
      left: 0px;
      top: 0fpx;
      height: 48px;
      width: 48px;

      @include rounded(15px);
    }
  }
}

