'use strict';

describe('controllers', function(){
  var scope;
  
  beforeEach(module('orienteerio'));

  beforeEach(inject(function($rootScope) {
  	scope = $rootScope.$new();
  }));


});
